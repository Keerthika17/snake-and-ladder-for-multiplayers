import React from 'react'

const Emoji = ( Props) => {
  return (
    <div>
        <h1>
            ({Props.playerValueContain.map(num=>num)}==={Props.id}){

            }
        </h1>
    </div>
  )
}

export default Emoji



